export class Member {
    UID: string;        //
    REALNAME: string;   //真实姓名
    USERTYPE: string;   //
    DEPTNAME: string;   //组织名称
    USERCODE: string;   //用户code
    CHECKED: boolean = false; // 节点是否被选中
    // STATE: number = 0; 
}